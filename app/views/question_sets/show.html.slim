.container
  = render partial: 'questions', locals: { question_set: @question_set }
  = link_to 'New Question', { controller: 'question', action: 'new', question_set_id: "#{question_set.id}", remote: true }, { id: "new-question-button", class: "btn btn-lg btn-primary" }
